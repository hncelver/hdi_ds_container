_schema-version: "2.1"
ID: hdi_ds_container
version: 0.0.1
modules:
- name: db
  type: hdb
  path: db
  requires:
  - name: HDI_DB
    properties:
      TARGET_CONTAINER: ~{hdi-container-name}
resources:
- name: HDI_DB
  type: com.sap.xs.hdi-container
  properties:
    hdi-container-name: ${service-name}
